color_coded:
 must compile to install:
 git clone https://github.com/jeaye/color_coded
 mkdir build && cmake .. && make && make install
 after intall:
 cp -r after/ ~/.vim/
 cp -r autoload/ ~/.vim/
 cp -r colors/ ~/.vim/
 cp -r plugin/ ~/.vim/
 cp color_coded.so ~/.vim/

 using by adding .color_coded include header file dir




